import React, { Fragment } from 'react'
import spinner from './spinner.gif'
/**
* @author
* @function Spinner
**/

const Spinner = (props) =><Fragment>
        <img src={spinner} alt="Loading ..." style={{width:"200px",margin:'auto',display:'auto'}}></img>
    </Fragment>


export default Spinner